# LiReal 部署上線

目標：把 `C:\Users\dmsou\Lireal` 的內容推到既有的 GitHub repo
（`dmsoul76823-bit/likereal-app`），Vercel 自動重新部署。

---

## 步驟 0（選做，1 分鐘）備份舊版程式碼

force push 會覆蓋 repo 的主分支。想保留舊的 Vite 版本：

1. 開 https://github.com/dmsoul76823-bit/likereal-app
2. 點左上角分支下拉選單（顯示 `main`）
3. 輸入 `old-vite-version` → 點 **Create branch: old-vite-version from main**

這樣舊程式碼就永久保留在那個分支了。

---

## 步驟 1：推上 GitHub

按 **Win + R** → 輸入 `cmd` → Enter，然後**一行一行**貼上執行：

```
cd /d C:\Users\dmsou\Lireal
```

```
git init
```

```
git add .
```

```
git commit -m "LiReal 沉浸式場域 - Supabase 版"
```

```
git branch -M main
```

```
git remote add origin https://github.com/dmsoul76823-bit/likereal-app.git
```

```
git push -u origin main --force
```

最後一行如果跳出登入視窗，選 **Sign in with your browser** 完成授權。

> `--force` 是必要的：遠端是舊的 Vite 專案，歷史不同，不加會被拒絕。

---

## 步驟 2：改 Vercel 設定（很重要）

舊專案是 Vite，會執行 `npm run build`。新版是純靜態網站，**不改會部署失敗**。

1. 開 https://vercel.com/dashboard → 點 **likereal-app** 專案
2. 上方 **Settings** → 左側 **Build and Deployment**
3. **Framework Preset** → 改成 **Other**
4. **Build Command** → 把開關切成 **Override**，欄位**留空**
5. **Output Directory** → **Override**，欄位**留空**
6. **Install Command** → **Override**，欄位**留空**
7. 按 **Save**

---

## 步驟 3：重新部署

1. 專案頁面上方 **Deployments**
2. 最新那筆右邊 **⋯** → **Redeploy** → 確認
   （如果步驟 1 push 完已經自動觸發部署，等它跑完就好）
3. 狀態變成綠色 **Ready** 即完成

---

## 步驟 4：驗收

打開你的網址（例如 `https://likereal-app.vercel.app`）：

| 網址 | 應該看到 |
|---|---|
| `/` | LiReal 入口頁 |
| `/app` | 前台網站 |
| `/admin` | 後台登入頁 |

**實測一輪：**

1. `/admin` 用你的 Supabase 帳號登入 → 資料都在（因為存在雲端）
2. 後台改一下跑馬燈 → 儲存
3. 換一台裝置或手機開 `/app` → **看得到你剛改的內容**
   （這就是階段②的意義：資料真正共用了）
4. 手機上買一張票 → 回後台看到訂單

---

## 之後要更新網站

改完 `C:\Users\dmsou\Lireal` 裡的檔案後：

```
cd /d C:\Users\dmsou\Lireal
git add .
git commit -m "更新說明"
git push
```

Vercel 會自動偵測並重新部署，大約 1 分鐘。

---

## 注意事項

- `config.js` 裡的 publishable key 會被公開 —— **這是正常的**，Supabase 的前端金鑰本來就設計成公開，真正的防護是資料庫的 RLS 規則。
- `.vercelignore` 已排除 `.sql`、`.bat`、`server.js`，這些不會被放到網路上。
- 目前**沒有金流**，前台結帳會直接產生「已付款」訂單。正式收費前要先串綠界。
